# Overfit diagnostic: Core and Megatron on 16 fixed GSM8K prompts

> Historical evidence. For current operating instructions, start at the [MILES guide](../index.md).

A positive update-correctness check: train repeatedly on a tiny fixed prompt set
through the fully asynchronous path and require training reward to rise. Both
arms rose. Neither reached 1.0 within 60 updates at the recipe learning rate.
This is a mechanics diagnostic, not a learning comparison or a throughput
qualification; see the limitations below.

| Arm | Beaker | Image | Config | Exit |
|---|---|---|---|---|
| Core (open-instruct MILES, OLMo-core trainer, refresh + TIS) | [01M2M1FR7K18Y7X43WBPEDDXBC](https://beaker.org/ex/01M2M1FR7K18Y7X43WBPEDDXBC) | `01M2F1RKZFZVJYAS0XQGEC3SEJ` | [core.toml](overfit-20260915/core.toml) | 0, workflow `complete` |
| Megatron (olmo-miles `compatibility` backend, fully async + TIS) | [01M2M1H0M7493XECRH4QP0T1X9](https://beaker.org/ex/01M2M1H0M7493XECRH4QP0T1X9) | `01M26TT1EQTB84RV0MM650W0YE` (r3 parity candidate) | [megatron.toml](overfit-20260915/megatron.toml), olmo-miles `f45f2433` | 0, lifecycle report written |

Shared recipe: the full-SFT KDA/latent-MoE checkpoint used by the
[100-update comparison](gsm8k-results-20260911.md); three B300 GPUs per arm
(two EP2 trainers, one TP1 engine); 16 training prompts; 8 prompts x 4 responses
per update (32 samples), so each prompt recurs every second update and 30 times
over the 60 updates; LR 1e-6 constant, no KL, no GRPO std normalization,
clipping 0.2/0.28; 4096 response / 6144 context; greedy 32-row held-out at
updates 0/20/40/60; W&B `ai2-llm/olmo-rl-comparison` group
`overfit-gsm8k-20260915`. Each launcher selected its own 16 training and 32
held-out rows from seed 17, so the prompt sets differ between arms; the pass
criterion is per arm.

## Training reward by block of updates

Per-update series: [core-series.csv](overfit-20260915/core-series.csv),
[megatron-series.csv](overfit-20260915/megatron-series.csv),
[core-train-series.csv](overfit-20260915/core-train-series.csv).

| Updates | Core reward | Core tokens | Core truncated | Megatron reward | Megatron tokens | Megatron truncated |
|---|---:|---:|---:|---:|---:|---:|
| 0-12 | 0.724 | 2166 | 20.2% | 0.798 | 1844 | 10.8% |
| 13-24 | 0.734 | 2105 | 21.9% | 0.737 | 2013 | 16.1% |
| 25-36 | 0.768 | 2006 | 16.1% | 0.802 | 1898 | 12.2% |
| 37-48 | 0.831 | 1834 | 12.0% | 0.820 | 1744 | 9.6% |
| 49-59 | 0.847 | 1652 | 9.1% | 0.849 | 1680 | 8.5% |

Both arms rise by about 0.12 (Core) and 0.05 (Megatron) from their first block
to their last, with response length and the truncation rate falling in step.
Per-batch reward tracks truncation closely on both arms: batches with 0-3%
truncated responses score 0.88-1.0, batches with 25-31% score 0.53-0.63. The
learned behaviour is finishing under the cap. Core reports group-level reward
statistics: groups with all-zero reward fell from 10.6% of groups (block 0-12)
to 1.1% (block 49-59) while all-one groups rose from 51% to 61%, leaving roughly
35-45% of groups carrying gradient throughout. Megatron does not log that
statistic.

Held-out (32 rows each, different rows per arm):

| Update | Core correct /32 | Core tokens | Megatron correct /32 | Megatron tokens |
|---|---:|---:|---:|---:|
| 0 | 27 | 1807 | 21 | 1920 |
| 20 | 28 | 1613 | 21 | 1755 |
| 40 | 26 | 1516 | 24 | 1578 |
| 60 | 27 | 1613 | 25 | 1602 |

Thirty-two rows cannot resolve these changes; the held-out numbers are a side
view showing shorter responses without collapse, not a learning result.

## Trainer-side health

| Block | Core grad norm | Core train/rollout log-prob gap | Megatron grad norm | Megatron gap |
|---|---:|---:|---:|---:|
| 1-12 | 0.184 | 0.0085 | 0.899 | 0.0061 |
| 13-24 | 0.184 | 0.0100 | 0.559 | 0.0062 |
| 25-36 | 0.202 | 0.0100 | 0.381 | 0.0061 |
| 37-48 | 0.151 | 0.0094 | 0.237 | 0.0060 |
| 49-60 | 0.159 | 0.0096 | 0.224 | 0.0060 |

Both gaps stay far inside the 0.05 policy-drift bound; TIS clip fraction is
zero on both arms. PPO clip fraction and PPO KL are identically zero on both
arms because each collection receives one optimizer step and the old
log-probabilities are the same forward, so the objective reduces to centred
REINFORCE. Core's router auxiliary losses were flat (0.194 -> 0.193). The two
gradient-norm columns use each trainer's own reduction and are not comparable
across arms; Megatron's steady decay across the run is recorded, not explained.

Core's asynchronous path was genuinely exercised: average learner staleness
0.6-2.0 updates, 40-60% of responses spanned a policy publication
(`mixed_version_ratio`) from update 3 onward, and the completed queue dropped
0.9% of samples in the first block and none afterwards. Megatron ran at
staleness 1 with no rejected policy groups.

## Wall clock

| Arm | First to last update | Median update gap | Mean gap, updates 0-9 | Mean gap, updates 50-59 |
|---|---:|---:|---:|---:|
| Core | 43.2 min | 35 s | 88 s | 30 s |
| Megatron | 63.3 min | 56 s | 75 s | 60 s |

Same batch shape, prompt budget and GPU count. This reverses the direction of
the [September 11 timing](gsm8k-configuration-differences-20260911.md), where
Core's warm cycle was 1.33x Megatron's; the Core arm here uses packing, no
activation recomputation and the skipped scoring pass qualified on
[September 14](full-sft-basket-20260914.md), while the Megatron arm keeps the
r3 recipe (recompute on, 6144 context, no packing). Not a matched throughput
measurement.

## Controls (September 16, Core path only)

Four cheap follow-ups on the Core arm, each isolating one way the result above
could mislead. Same 16 prompts, checkpoint, batch shape and three-GPU layout;
configs, per-update series, the re-score script and its report are under
[controls/](overfit-20260915/controls/).

| Control | Beaker | Question | Outcome |
|---|---|---|---|
| Offline re-score of the 1,920 retained rollouts (Saturn CPU) | [01M2MDDPAKZZ5F8366Z7ZW8BAT](https://beaker.org/ex/01M2MDDPAKZZ5F8366Z7ZW8BAT) | Did the verifier score what the run says it scored? | 0 mismatches; recomputed mixed-group fractions equal the in-run `zero_std` statistics; 28 truncated responses (1.5%) were rewarded because their last number matched |
| Null control, LR 1e-12, 40 updates | [01M2MK7P67350K83R77HRVQZJV](https://beaker.org/ex/01M2MK7P67350K83R77HRVQZJV) | Do reward or weights move without an optimizer step? | Flat: block means 0.72/0.70/0.70/0.70, tokens 2030-2211, trainer/rollout gap 0.0094 -> 0.0097; held-out 26/26/24 |
| Sign flip, verifier weight -1, 40 updates | [01M2MDQJPQS4GN3637MY3VAJTT](https://beaker.org/ex/01M2MDQJPQS4GN3637MY3VAJTT) | Does the update follow the reward, or drift toward short responses regardless? | Accuracy fell 0.73 -> 0.57 by block; all-wrong groups 6% -> 23%; held-out 28 -> 26 -> 22 of 32 |
| Learning-rate ladder, LR 5e-6, 60 updates | [01M2MDBM7EXCGC482DHQQY5TNX](https://beaker.org/ex/01M2MDBM7EXCGC482DHQQY5TNX) | Is the slow rise at 1e-6 learning-rate bound? | 0.76 -> 0.95 by block 40-49; truncation 21% -> 1%; tokens 2077 -> 785; held-out 27 -> 28 at 882 tokens |

A first null attempt at LR 0 ([01M2MD954QZZQ67B65989K2441](https://beaker.org/ex/01M2MD954QZZQ67B65989K2441))
failed at optimizer construction: the OLMo-core MoE optimizer asserts `lr > 0`,
and `validate` does not reject it. The 1e-12 rerun moves each weight by less
than bf16 resolution over 40 Adam steps and is the effective null.

Block means of training accuracy (sign flip shown as accuracy, i.e. minus its reward):

| Updates | Null 1e-12 | Original 1e-6 | Ladder 5e-6 | Sign flip |
|---|---:|---:|---:|---:|
| 0-9 | 0.716 | 0.72 | 0.756 | 0.731 |
| 10-19 | 0.697 | 0.73 | 0.878 | 0.688 |
| 20-29 | 0.703 | 0.77 | 0.884 | 0.666 |
| 30-39 | 0.703 | 0.81 | 0.903 | 0.569 |
| 40-49 | | 0.83 | 0.953 | |
| 50-59 | | 0.85 | 0.931 | |

The null run gives the noise floor: per-update reward standard deviation 0.092
at fixed weights, so a ten-update block mean has a standard deviation near 0.03.
The original run's rise of 0.13 and the sign flip's fall of 0.16 are both well
outside it; the null's own blocks stay within 0.02 of each other. Greedy held-out
on the identical checkpoint scored 26/32 in the null run and 27/32 in the two
sibling runs, so one question flips between engine instances; that is the floor
for reading single-question changes.

Length under the sign flip fell (2145 -> 1660 tokens) rather than rising, while
truncation stayed at 18-21% instead of falling as it did in the original. A
short finished wrong answer earns the flipped maximum just as a truncated one
does, so this is consistent with the policy finding the cheapest zero-reward
behaviour; it does not by itself exclude a mild reward-independent length
preference, which a flip that rewards hitting the cap would test if needed.

Trainer-side health was unchanged across all three GPU controls: trainer/rollout
log-prob gap 0.009-0.012 against the 0.05 bound, TIS clip fraction below 2e-5,
router auxiliary loss 0.192-0.194. The sign-flip gradient norm rose from 0.21 to
0.37 over its run while the ladder run's fell from 0.15 to 0.10; the null held
0.25 -> 0.20.

What these establish together: the verifier scores what it reports; nothing
moves without an optimizer step; the update moves accuracy in the direction of
the reward on both training and held-out prompts; and at a larger step the fixed
set is solved inside 40 updates with no health metric moving. What they do not
establish: anything about the Megatron path (all controls are Core), between-seed
variance of a learning run, or the equivalence of the async path to a synchronous
one.

## Limitations

- One seed per arm, different prompt sets, 32 samples per update. Block means
  are the readable signal; single updates swing by 0.3.
- The recipe learning rate was kept deliberately. Neither arm reached 1.0 in
  60 updates; the rise became visible only after about update 30. A faster
  learning rate would give a sharper diagnostic at the cost of comparability.
- Plain GSM8K at 0.8 initial reward is partly saturated for this checkpoint:
  half the groups were all-correct from the start and carried no gradient.
- Trainer-side entropy is not logged on the Core arm; Megatron's
  `entropy_loss` is zero because the entropy coefficient is zero.
- Controls were run on the Core path only, one seed each.
